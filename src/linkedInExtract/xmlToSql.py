#!/usr/bin/python

import sys

from django.contrib import admin
from models import Student

class StudentAdmin(s):
    new_entry = Student(user = s.id, first_name = s.first-name,last_name =
            s.last-name, 









StudentAdmin(sys.argv[1])

